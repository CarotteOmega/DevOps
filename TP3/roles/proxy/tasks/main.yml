---
# tasks file for roles/proxy
- name: Run HTTPD
  docker_container:
    name: httpd
    image: carottealpha/tp-devops-simple-api-httpd
    ports: 
      - "8080:80"
  vars:
    ansible_python_interpreter : /usr/bin/python3
    
